{
  "mcpServers": {
    "postmancer": {
      "command": "npx",
      "args": ["-y", "postmancer"],
      "env": {
        "LOG_LEVEL": "info",
        "COLLECTIONS_PATH": "%USERPROFILE%\\.postmancer"
      }
    }
  }
}